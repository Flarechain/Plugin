#include "ColorPalette.h"

// Coffee
const auto ColorPalette::Coffee500 = juce::Colour::fromRGB(35, 5, 15);

// Red
const auto ColorPalette::Red500 = juce::Colour::fromRGB(254, 0, 8);

// Orange
const auto ColorPalette::Orange500 = juce::Colour::fromRGB(252, 87, 5);

// Yellow
const auto ColorPalette::Yellow500 = juce::Colour::fromRGB(254, 154, 6);

// Linen
const auto ColorPalette::Linen100 = juce::Colour::fromRGB(255, 246, 235).withAlpha(0.2f);
const auto ColorPalette::Linen200 = juce::Colour::fromRGB(255, 246, 235).withAlpha(0.3f);
const auto ColorPalette::Linen300 = juce::Colour::fromRGB(255, 246, 235).withAlpha(0.4f);
const auto ColorPalette::Linen400 = juce::Colour::fromRGB(255, 246, 235).withAlpha(0.9f);
const auto ColorPalette::Linen500 = juce::Colour::fromRGB(255, 246, 235);
const auto ColorPalette::Linen600 = juce::Colour::fromRGB(241, 203, 164);
const auto ColorPalette::Linen700 = juce::Colour::fromRGB(217, 183, 148);